<nav class="ah_navigation">
	<div class="ah_navigation-logo_wrapper">
		<button type="button" value="mv" class="scrollButton">
			<img class="ah_navigation-logo" src="dist/assets/logo.svg" alt="Ayaka Hoshino's Portfolio">
		</button>
	</div>
	<ul class="ah_navigation-items">
		<% var ary = ['About', 'Skills', 'Works', 'Other', 'Contact']; %>
		<% ary.forEach(function (value, key) { %>
		<li class="ah_navigation-item">
			<button value="<%= value %>" type="button" class="scrollButton"><%= value %></button>
		</li>
		<% }); %>
	</ul>
</nav>